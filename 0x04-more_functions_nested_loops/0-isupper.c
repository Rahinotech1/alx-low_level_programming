#include "main.h"
#include <stdio.h>
/**
 * _isuper - Evaluate if letter is uppercase.
 * @c: print int
 * Return: Always 0.
 */

int _isuper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}

}
